import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains:[
      'https://deisishop.pythonanywhere.com',
      "deisishop.pythonanywhere.com"
    ]
  }
};

export default nextConfig;
